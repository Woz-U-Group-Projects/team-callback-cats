let express,mongoose,cors,readdirSync;_fad‍.w("express",[["default",["express"],function(v){express=v}]]);_fad‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);_fad‍.w("cors",[["default",["cors"],function(v){cors=v}]]);_fad‍.w("fs",[["readdirSync",["readdirSync"],function(v){readdirSync=v}]]);



const morgan = require('morgan');

require("dotenv").config();

const app = express();

//database
mongoose
  .connect(process.env.DATABASE, {
    useNewUrlParser: true,
    useUnifiedTopology: true,
    useFindAndModify: false,
    useCreateIndex: true,
  })
  .then(() => console.log(`DB Connected`))
  .catch((err) => _fad‍.g.console.log("DB connection error", err));


//middleware
app.use(cors());
app.use(morgan("dev"));

//routes middleware
readdirSync('./routes').map((r) => app.use('/api', require(`./routes/${r}`)));


const port = process.env.PORT || 4000;
app.listen(port, () => console.log(`Server is running on port ${port}`));
